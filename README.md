# Joplin to Obsidian

一个将 Joplin 笔记导入到 Obsidian 的插件，支持文件夹层级结构和资源文件（图片、附件等）。

## 功能特性

- ✅ 导入指定 Joplin 笔记本及其所有子笔记本
- ✅ 保持原有的文件夹层级结构
- ✅ 自动处理图片和附件资源
- ✅ 将 Joplin 资源链接转换为 Obsidian 格式
- ✅ **图片自动重命名**：按 `笔记名-001.ext` 格式命名，保持风格统一
- ✅ 可配置输出目录和附件文件夹名称
- ✅ 设置界面一键导入

## 安装方法

### 方式一：从 GitHub Release 安装（推荐）

1. 前往 [Releases](../../releases) 页面下载最新版本
2. 下载以下文件：
   - `main.js`
   - `manifest.json`
3. 在你的 Obsidian 库中创建插件目录：`.obsidian/plugins/joplin-to-obsidian/`
4. 将下载的文件复制到该目录
5. 重启 Obsidian 或刷新插件列表
6. 在设置中启用「Joplin to Obsidian」插件

### 方式二：手动安装

```bash
cd /path/to/your/vault/.obsidian/plugins
git clone https://github.com/你的用户名/joplin-to-obsidian.git
cd joplin-to-obsidian
npm install
npm run build
```

## 使用方法

### 1. 关闭 Joplin

在导入前，请先**关闭 Joplin 应用**，避免数据库被锁定。

### 2. 配置插件

打开 Obsidian 设置 → 第三方插件 → Joplin to Obsidian，配置以下选项：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| Joplin 数据库路径 | Joplin SQLite 数据库文件路径（支持 `~`） | `~/.config/joplin-desktop/database.sqlite` |
| Joplin 资源目录 | Joplin 资源文件（图片、附件）目录（支持 `~`） | `~/.config/joplin-desktop/resources` |
| 要导入的笔记本名称 | Joplin 中顶级笔记本的名称 | `joplin` |
| 输出文件夹 | Obsidian vault 中的目标文件夹 | `joplin` |
| 图片文件夹名称 | 存放图片的文件夹（统一放在 vault 根目录下） | `assets` |

> 💡 **提示**：如果你的 Joplin 笔记本名称就是 `joplin`，则无需任何配置，直接点击导入即可！

### 3. 导入笔记

有两种方式触发导入：

1. **设置页面**：点击「导入笔记」按钮
2. **命令面板**：按 `Cmd/Ctrl + P`，搜索「从 Joplin 导入笔记」

### 4. 查看结果

导入完成后会显示成功和失败的笔记数量。笔记会按照 Joplin 中的文件夹结构导入到指定目录。

## Joplin 数据位置

插件默认使用 `~/.config/joplin-desktop/` 路径（macOS/Linux），大多数用户无需修改。

| 操作系统 | 数据库路径 | 资源目录 |
|----------|-----------|----------|
| macOS/Linux | `~/.config/joplin-desktop/database.sqlite` | `~/.config/joplin-desktop/resources` |
| Windows | `%APPDATA%\joplin-desktop\database.sqlite` | `%APPDATA%\joplin-desktop\resources` |

> Windows 用户需要手动修改路径为 `C:\Users\用户名\AppData\Roaming\joplin-desktop\...`

## 导入后的目录结构

假设 Joplin 中的笔记本结构为：

```
我的笔记/
├── 工作/
│   ├── 项目A.md
│   └── 项目B.md
└── 生活/
    └── 日记.md
```

导入后在 Obsidian 中的结构（假设输出文件夹设为 `joplin`）：

```
vault/
├── assets/              # 图片统一放在 vault 根目录的 assets
│   ├── 项目A-001.png    # 图片按笔记名-序号命名
│   ├── 项目A-002.jpg
│   └── 日记-001.png
└── joplin/              # 笔记放在输出文件夹
    ├── 工作/
    │   ├── 项目A.md
    │   └── 项目B.md
    └── 生活/
        └── 日记.md
```

图片链接使用简写路径（如 `![](项目A-001.png)`），Obsidian 会自动从 `assets` 目录查找。

## 注意事项

- **导入前请关闭 Joplin**：Joplin 运行时会锁定数据库，导致无法读取
- **备份数据**：建议在导入前备份 Obsidian vault
- **仅支持桌面版**：由于需要访问文件系统，此插件仅支持桌面版 Obsidian
- **资源文件**：只有在笔记中被引用的资源才会被复制到 attachments 文件夹

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建
npm run build

# 部署到本地 vault
npm run deploy

# 发布到 GitHub
npm run release
```

## 技术栈

- TypeScript
- Obsidian API
- sql.js (WebAssembly SQLite)
- esbuild

## 许可证

MIT
